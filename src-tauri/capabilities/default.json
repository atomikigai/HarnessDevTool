{
  "$schema": "https://schema.tauri.app/capability/2.0.0.json",
  "identifier": "default",
  "description": "Default permissions for HarnessDevTool desktop",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default",
    "shell:allow-spawn",
    "shell:allow-kill"
  ]
}
